import styles from './Button.module.css'

export default function Button({ data }) {
	// const { btn, id, classes } = data

	// return (
	// 	<span className={ }>
	// 		<span>{btn.title}</span>
	// 	</span>
	// )
}